# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Other stuff
4-challenge/
.direnv
*.log
# Data is getting downloaded when pipeline is run anyway
data/ 
# The raw json files are way too large to be handled by git so we never commit them
# If raw data is needed just run the pipeline 
#(or i will find a way to save artifacts like this later)
raw_data/*.json
